# SSH credentials for lab routers
SSH_USERNAME=vyos
SSH_PASSWORD=vyos

# Optional: default SSH port if not overridden per device in inventory
SSH_PORT=22

# Logging settings
LOG_LEVEL=INFO

# Output directories
CONFIGS_DIR=configs
GENERATED_DIR=configs_generated
BACKUP_DIR=backups
